To get you some hands-on experience with Cloud Code, I created a small project for us to work on. You should find the source for this project in a zip file attached to this lecture. I would encourage you to download this zip, extract its contents, and then start up your code editor inside of this new project directory. I have already opened up the project inside of my editor. Now once you have the project open, you might be tempted to look through the project contents and maybe go through a little bit of setup which is described inside the README file. But before you do, there's something important I would like you to understand about Cloud Code. Cloud Code is not a tool that is just going to write code for you. Naturally, it absolutely can, but that's not the only thing that Cloud Code is about. Instead, you really want to view Cloud Code as another engineer who is working on this project alongside you. Every task that you would normally go through on a normal project can really be fully delegated off to Cloud. So this includes everything from initially setting up a project, to designing new features, to deployment, and to support. As we go through this project, we're going to leverage Cloud heavily to aid us in various steps. We will use Cloud to set up the project for us, to plan out a new feature, to write tests and code, and then later on, on a slightly different project, I'll show you how we can use Cloud Code to automatically discover and fix errors in a production environment. So let's get to it. Back inside my editor, I'm going to open up my terminal and then launch Cloud Code inside there by executing clod. Then, once I have this open, I'm going to put in my first directive to Cloud. I'm going to ask it to read the contents of the README file and go through any setup directions that are listed inside of here. I will ask Cloud to read the contents of the README file and execute the setup directions listed in it. Cloud will then use a variety of different tools to read that file and then execute a series of different commands. It will create a new virtual environment, activate the environment, and then install some dependencies. Once that is complete, we are going to run a command that will help Cloud get a better understanding of our project. We're going to do so by running the init command. This is a command that we're going to execute inside of Cloud Code itself. When you execute this command, Cloud will automatically scan your codebase to understand your project's general architecture, coding style, and so on. Once complete, Cloud will write all of its findings into a special file named clod.md. Whenever we run Cloud again in the future, this file will be automatically included as context. Just so you know, there are three different Cloud files, project, local, and user. We're going to see some references to these in a minute, so we'll discuss what they're all about then. Whenever you run this init command, you can also optionally add in some special directions for some areas for Cloud to focus on. Let's try this out right now and see what is generated for our project. I'm going to run the init command, and when I do, I'm going to pass in some special instructions. I'm going to ask Cloud to include some detailed notes on defining MCP tools. Once it is all done, I'm going to take a look at the newly generated clod.md file. This is a summary of what Cloud thinks of our codebase. At the top, it will list out some important commands that it might need to run in the future. We'll get some listing around our coding style that we've used inside this project. And then as I specially requested, it also included some information around defining MCP tools. As I mentioned a moment ago, this file is going to be included as context for Cloud in any follow-up requests we make in the future. Now, projects change over time. We might change our coding style or add in some additional commands. So if that ever happens, we can very easily manually edit this file, or we can choose to rerun the init command. If you rerun this command, Cloud will update the contents of the clod.md file. Finally, as a very small shortcut, we can put in a pound and then type in some specific note that we want to be appended into the contents of that file. So we can use this as a tool to give very specific small directions to Cloud that will be included in all follow-up requests. So for me, I might add in a direction here that says something like, Always apply appropriate types to function args. And if I run that, I'll then be asked where I want to add this little note. This is where we see that project memory, local memory, and user memory appear. In my case, this is a note that I want to be shared with everyone who is working on this project. So I will add it to project memory. Once I add that in, I can then check the contents of my clod.md file. And either somewhere under a code style or perhaps at the very bottom, I'll probably see whatever note I just added in. At this point, we have added a new file to our project. And this project is being managed by Git. Normally, we would open up our terminal, stage this new file that we just created into Git, and then commit those changes. We could do all that manually, but it would be a lot faster if we just asked Cloud to do it for us. So I will ask Cloud to stage and commit all changes. Cloud will then take a look at all the different changes we have made to the codebase, write a descriptive commit message, and commit those files. Next up, I would like to show you some techniques for increasing Cloud's effectiveness when writing code. We are going to add a new feature into this project. As a reminder, this project is a very small, very simple MCP server. We are going to ask Cloud to add in a new tool to the server that will read a Word document or a PDF file, and convert the contents to Markdown. Now we absolutely could just type in directions for that to Cloud, something like make a Word doc plus PDF file to Markdown conversion tool. But before doing that, I want you to know that we can dramatically increase the effectiveness of Cloud by putting in a little bit more effort. So let me show you how. Think of Cloud Code as being an effort multiplier. If you put a little effort into how you direct Cloud, you will get back significantly better results. I am going to show you two different workflows, two different ways of instructing Cloud on how to approach a task. Both of these workflows require a little bit of effort on your side, but they allow Cloud to tackle much more complex problems. In this first workflow, we are going to go through three distinct steps. First, we are going to identify some areas of our codebase that we know are relevant to a feature that we are trying to work on. We are then going to ask Cloud to specifically read and analyze those files. Second, we are going to tell Cloud about the feature we want to build and ask it to plan out a solution. So the steps will actually go through to implement whatever feature or problem you are trying to solve. And then finally, after Cloud has gone through that planning step, we will ask Cloud to actually implement the solution. Let me show you how we would do this for our particular feature of adding in some new document conversion tool. So first, I am going to go through my codebase and identify some different relevant files. You will notice there is a tools directory and inside there is a math.py file. This is an example of a tool that has already been put together. So this might be some file that is relevant for the feature that we are trying to build, just because it gives Cloud a better idea of how to author a tool. Second, we might ask Cloud to take a look at the document.py file, which includes a very helpful function, binary document to markdown. So this will take in some amount of binary data and convert it to markdown. So we can tell Cloud to take a look at these files and Cloud will get a better idea of how to write a tool in the first place and then how to actually do the conversion. So I am going to add in some instructions to Cloud and ask it to just read the contents of those files. Next, I am going to ask Cloud to plan out the implementation of a new tool called Document Path to Markdown, which will take in a path to a PDF or Word document, read the file, convert the contents to markdown, and return the result. I am also going to tell Cloud specifically to just plan the feature out and not to write any code just yet. In response, Cloud is going to give us a pretty well detailed plan that is going to go through a couple of different steps that will be required to implement this entire thing. Finally, I will ask Cloud to implement this plan. It's first going to update the document.py file, which is definitely correct, it's then going to update the main.py file, which is also good, and then finally it will author a test around this new tool. It's then going to run the test suite and make sure that the test actually pass. You'll notice that in the summary message, Cloud also tells me that it added in some error handling for non-existent or unsupported file types, which was definitely appropriate even though it wasn't something that I actually asked for explicitly inside of my description of this feature. I would like to show you another way that we could have asked Cloud to implement this feature. Before I show you this alternative, I am going to get Cloud to remove all the code that it just wrote by stashing these changes. Now I am back to a clean slate where I don't have anything related to that new feature that was just implemented. The second technique that we are going to explore is a test-driven development workflow. This is going to take a lot of effort from you up front, but it dramatically increases Cloud's effectiveness. With this workflow, we will again ask Cloud to take a look at some relevant contexts. Then, before writing any code, we are going to ask Cloud to think of some different tests it could write related to our feature. Next, we will select the most relevant tests and ask Cloud to implement them. Finally, after we have got some working tests put together, we will ask Cloud to write out a solution and write code until the tests actually pass. So, let me show you how this approach would actually look for the same exact feature, one where we are building a tool that is going to read a document off of our hard drive and convert its contents into Markdown. First, I am going to run the slash clear command. This is going to clear up my conversation history with Cloud, essentially reset its context. In this case, I am doing this just so it doesn't cheat off of its previous solution. Then I will ask Cloud to read those two relevant files once again. Now, I am going to run the slash clear command again. This time, I am going to ask Cloud to read the text that I have written. Again, I am going to ask it specifically to not write any code just yet. The suggestions I get back are really solid. Now, I don't really want to have to worry about tests 6, 7, and 8 because those are a little bit more specialized beyond what we are doing right now. So, I am going to ask Cloud to just implement tests 1 through 5. Now, we will move on to the last step, where we will ask Cloud to write out some code to make these tests pass. Very good, all the tests pass. Now, before we move on, I just want to remind you one more time that Cloud code really is an effort multiplier. You can put in very simple directions, and Cloud will do its best, but you can dramatically increase Cloud's effectiveness by putting in a little bit of effort on your side as well.