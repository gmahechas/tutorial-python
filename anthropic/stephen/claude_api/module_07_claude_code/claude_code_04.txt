In this video, I want to show you one of the most interesting aspects of Cloud Code. Cloud Code has an MCP client embedded inside of it. That means that we can connect MCP servers to Cloud Code and dramatically expand its functionality. To demonstrate this, we are going to connect Cloud Code to the MCP server that we have been working on. We just authored a tool called Document Path to Markdown. We can now expose this tool to Cloud Code, allowing it to read the contents of PDF and Word documents. So we are dynamically expanding the capabilities of Cloud Code. Let me show you how to set this up. Back inside of my terminal, I'm going to end my running session with a Control-C. I'm then going to add in an MCP server to Cloud Code by executing clod mcp add. Then we're going to put in the name for our MCP server. Now the name could be anything you want it to be. In our case, we are making a server related to Documents, so I'm going to call our server Documents. And then finally, we're going to put in the command that we use to start up our server, which for us is uv run main.py. So uv run main.py. And that's it. I'm going to execute that, and I'll start Cloud Code back up with Cloud. And now we can make use of that tool that we just put together. To really test it out, inside of our test directory, there is a Fixers folder. Inside there are two demo files, so a Word doc and a PDF doc. Both contain just a tiny bit of documentation around MCP itself. So I'm now going to ask Cloud to convert the contents of either one of those files into Markdown. And my expectation is that Cloud will make use of that tool that we just authored a moment ago. And if we scroll up just a little bit here, sure enough, it worked. So this actually is the contents of that file. This ability to consume MCP servers adds an incredible amount of flexibility to Cloud Code, and really opens the door to some really interesting development opportunities. For example, you might decide to add in a series of MCP servers related to your particular development flow. For example, if you use Sentry for production monitoring, you can add a Sentry MCP server to allow Cloud to fetch details about errors that are occurring in production. If you make use of Jira, you can add in an MCP server that will allow Cloud to view the contents of specific tickets. If you're a Slack user, you can add Slack to message you whenever Cloud is completed working on some particular problem. These are just a small fraction of the possible enhancements that you can add into Cloud Code. So it would definitely be worth your time to think about how you can enhance your particular development workflow.